buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.4.2'
    }
}

// Các cấu hình dự án được quản lý tập trung tại settings.gradle

task clean(type: Delete) {
    delete rootProject.buildDir
}
